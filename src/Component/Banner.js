var React = require('react');

const Banner = React.createClass({
	render:function(){
		return (	
			<div id="banner">
				<div id="banner-download-quote">Create visual history of your design on desktop.</div>
				<div id="banner-download-title">The Journey is the Reward</div>
			</div>
		)
	}
});

module.exports = Banner;